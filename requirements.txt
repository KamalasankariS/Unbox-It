numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
fastapi>=0.110
uvicorn>=0.27
pytest>=7.4
PyYAML>=6.0
httpx>=0.27          # required by fastapi.testclient (Starlette TestClient)

# Optional: enables the sentence-transformer article encoder
# (encoder.kind: sentence-transformer). The project runs without it — the import
# is guarded and falls back to TF-IDF+SVD.
# sentence-transformers>=2.2
